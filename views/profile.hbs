<h1>My Account</h1>

<div>
    <img src="{{user.picture}}" alt=" {{user.name}} picture" class="productImage">
</div>
    
<ul class="container">
    <li> Name: {{user.username}} </li>
    <li> Email Id: {{user.email}}</li>
    <li> Age: {{user.age}}</li>
    <li>Location: {{user.location}}</li>
</ul>

<h2>My Products</h2>

<ul class="container">
    {{#each userFavorite}}
        <li>
            <img src="{{product.imageUrl}}" alt=" {{product.name}}" class="productImage"/>
            <p> {{product.name}} by {{product.brand}} </p>
        </li>
    {{/each}}

</ul>

<h2>Your looks</h2>
<ul>
    {{#each lookCreated}}
    <a href=" "> {{name}} </a>
    {{/each}}
</ul>


<a href="/looks">Create a new look</a>